import { ThumbNailReaperPipe } from './thumb-nail-reaper.pipe';

describe('ThumbNailReaperPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbNailReaperPipe();
    expect(pipe).toBeTruthy();
  });
});
